<====================================Invertory File

all:
  hosts:
    web01:
      ansible_host: Private IP
      ansible_user: ec2-user
      ansible_ssh_private_key_file: clientkey.pem





<==============================Commond========>

 $ ansible web01 -m ping -i invertory

** m is module , ansible has a lots of modules
